<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title>Login Form</title>
    <style>
        body {
            background-color: #FFE4C4; /* Bisque background */
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .login-container {
            width: 100%;
            max-width: 400px;
            padding: 30px;
            background-color: #FEF8E7; /* Card background */
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            text-align: center;
        }
        .login-header {
            margin-bottom: 20px;
        }
        .login-header i {
            font-size: 60px;
            color: #08475C; /* Icon color */
                
        }
        .login-header h2 {
            margin: 0;
            color: #08475C; /* Primary text color */
        }
        .form-group {
            margin-bottom: 20px;
            text-align: left;
            position: relative;
        }
        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: #08475C; /* Label text color */
            font-weight: 500;
        }
        .form-group i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #08475C; /* Input icon color */
        }
        input[type="email"],
        input[type="password"] {
            width: 80%;
            padding: 12px 40px 12px 45px;
            border: 1px solid #dcdcdc;
            border-radius: 5px;
            font-size: 16px;
            background-color: #fff;
            outline: none;
            color: #333;
        }
        input::placeholder {
            color: #777;
        }
        input:focus {
            border-color: #08475C;
            box-shadow: 0 0 5px rgba(8, 71, 92, 0.5);
        }
        .login-btn {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 5px;
            background-color: #08475C; /* Button color */
            color: #fff;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .login-btn:hover {
            background-color: #2183AA; /* Hover button color */
        }
        .register-link {
            display: block;
            margin-top: 15px;
            color: #08475C;
            text-decoration: none;
            font-size: 14px;
        }
        .register-link:hover {
            text-decoration: underline;
        }
        .login-footer {
            margin-top: 15px;
            font-size: 14px;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-header">
            <i class="fas fa-user-circle"></i>
            <h2>Login</h2>
        </div>
        <form action="/auth/login" method="POST">
            <div class="form-group">
                <label for="email">Email Address</label>
                
                <input type="email" id="email" name="email" placeholder="Enter your email" required>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                
                <input type="password" id="password" name="password" placeholder="Enter your password" required>
            </div>
            <button type="submit" class="login-btn">Login</button>
            <a href="/register" class="register-link">Don't have an account? Sign up</a>
        </form>
        <div class="login-footer">
            <p>© 2024 Pixel Device Solution</p>
        </div>
    </div>
</body>
</html>

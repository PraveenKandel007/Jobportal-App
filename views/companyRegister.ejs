<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register New Company</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: bisque;
            color: #333;
        }
        .form-container {
            background-color: #fef8e7;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        }
        .btn-primary {
            background-color: rgb(8, 71, 92);
            border: none;
        }
        .btn-primary:hover {
            background-color: #2183aa;
        }
        footer {
            background-color: rgb(8, 71, 92);
            color: #fff;
            text-align: center;
            padding: 10px 0;
            margin-top:20.8%;
        }
    </style>
</head>
<body>

    <!-- Page Title -->
    <div class="container text-center mt-5">
        <h1 class="fw-bold text-dark">Register New Company</h1>
        <p class="text-muted">Fill out the form below to register your company.</p>
    </div>

    <!-- Registration Form -->
    <div class="container d-flex justify-content-center mt-4">
        <form action="/company/register" method="POST" class="form-container col-12 col-md-6">
            <div class="mb-3">
                <label for="companyName" class="form-label">Company Name:</label>
                <input type="text" id="companyName" name="companyName" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="address" class="form-label">Address:</label>
                <input type="text" id="address" name="address" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="contactInfo" class="form-label">Contact Info:</label>
                <input type="text" id="contactInfo" name="contactInfo" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Register Company</button>
        </form>
    </div>

    <!-- Footer -->
    <footer>
        &copy; 2024 Next Step Workforce Solutions. All rights reserved.
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
